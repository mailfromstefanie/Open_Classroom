# Worklog — Open Classroom — 2026-09-07 / 2026-09-08

## Purpose

Durable handoff for the stabilization work completed during the evening of 7/8 September 2026.

The real Unity project remains authoritative:

`E:/Projects/Open_Classroom/#Unity/Open_Classroom`

Stef also created:
- a fresh offline backup;
- a second backup stored online in OneDrive.

Treat this as a strong recovery point before the final planned Classroom additions.

## VRCDN / VideoTXL livestream investigation

The VRCDN livestream issue was investigated using the real VRChat client logs rather than ClientSim.

Important evidence:
- ClientSim is not a valid AVPro/VRCdn playback test because its AVPro video player is stubbed;
- therefore a livestream remaining in Loading in ClientSim is not evidence of a real VideoTXL or VRCDN defect;
- the real VRChat Build & Run log showed the VRCdn stream opening through VideoTXL using AVPro 1080p low-latency;
- the stream URL/backend route was correct;
- the physical video-wall renderer had become disabled and was re-enabled;
- warnings were also observed about allowed domains and temporary rate limiting when switching videos very rapidly.

Current practical status:
- the VRCDN route was checked using VRChat Build & Run;
- ordinary VideoTXL playback remains part of the protected working baseline;
- do not redesign or update VideoTXL based on ClientSim AVPro behaviour;
- keep exact VideoTXL 2.5.1.

## E-reader usability improvements

Both physical e-readers were refined.

Current reported behaviour:
- desktop/mobile users keep the controls open automatically after releasing the reader;
- visible pickup highlights were added along the reader sides;
- controls were redesigned with larger buttons, stronger contrast, hover states and pressed states;
- long labels were reduced to compact navigation symbols such as `|<`, `<` and `>`;
- page feedback was simplified;
- an automatic local sleep/close behaviour was added: after more than 30 seconds farther than approximately one metre from the e-reader, it closes locally.

## E-reader persistence

The e-reader now uses VRChat PlayerData for personal reading progress.

Reported behaviour:
- last-read page is stored per user;
- both physical e-readers use the same storage key/code;
- therefore the two physical readers share that user's reading progress;
- persistence is personal, not shared world state.

This is the first proven-in-project implementation pattern for VRChat PlayerData and is an important reference for future small per-user persistent settings.

A final real multiplayer check is still recommended before calling this fully accepted across multiple users.

## Marker Pro reset

Five Marker Pro objects received dedicated reset points.

Reported global reset behaviour:
- markers are force-released;
- returned for everyone to their intended original position;
- original rotation is restored.

A final joint multiplayer check is still recommended.

## Backup discipline

Separate backups were created before major changes during this work, including screenshots of the old and new e-reader UI.

After the session, Stef additionally created:
- one offline full backup;
- one online OneDrive backup.

Do not overwrite or casually discard this recovery point.

## Remaining acceptance test

Tomorrow / next session, before adding new features, perform one narrow multiplayer acceptance pass:

1. verify PlayerData e-reader page persistence with two users / reconnect as appropriate;
2. verify both physical e-readers correctly share the same user's saved progress;
3. verify Marker Pro global reset is seen correctly by both users;
4. optionally late-join once if useful.

If this passes, record it as the current golden Classroom baseline.

## Final planned Classroom additions

Stef considers the Classroom functionally complete apart from two final creator/teacher customization features.

### 1. Persistent entrance text

Desired experience:
- teacher can edit the text shown near the entrance;
- teacher's chosen text should persist across future visits;
- when that teacher is the first person establishing the new Classroom instance, their stored text should become the shared text for that instance;
- everyone already present and later joiners should see the same instance text.

Preferred architecture direction:
- PlayerData = the teacher's personal saved source value;
- small synced instance state = the current shared entrance text;
- load persistence only after VRChat reports the player's persistent data restored;
- do not continuously save while typing; use an explicit Apply/Save action;
- include Reset to Default.

Exact networking/authority behaviour must be implemented conservatively and tested in real multiplayer.

### 2. Persistent synchronized movable poster

Desired experience:
- teacher can paste a direct image URL, e.g. own server or a supported image host;
- poster image can be picked up and placed anywhere;
- pickup interaction should feel like the current e-reader pickup behaviour;
- UI styling/scale control may reuse the visual language of the existing local table screens, but the working table-screen system itself should not be modified;
- only a simple uniform size slider is needed;
- poster object should be synchronized so other users see its current placement;
- teacher's poster configuration should persist for future Classroom sessions.

Preferred persistent fields:
- image URL;
- position;
- rotation;
- uniform scale.

Preferred separation:
- PlayerData stores the teacher's personal persisted poster configuration;
- synchronized runtime state represents the poster actually active in the current instance;
- VRCObjectSync / ownership or another proven small authoritative transform path may be used for shared movement, but inspect existing project reset/pickup patterns first;
- late joiners must receive the current poster state;
- do not turn the local table screens into networked/persistent screens;
- do not couple this to VideoTXL or Presentation unless evidence shows a real need.

Potential reusable references:
- e-reader: pickup feel + existing PlayerData pattern;
- table screens: UI visual language and scaling concept only;
- Marker Pro/reset system: home/reset and ownership/release behaviour;
- existing reset infrastructure: only where responsibility fits.

## Hard boundary for next implementation

The Classroom is currently in a good working state.

Before either final feature:
- inspect current real Unity wiring first;
- preserve Presentation;
- preserve VideoTXL 2.5.1;
- preserve VRCDN playback;
- preserve e-readers;
- preserve table screens;
- preserve projector/display/readability;
- preserve Marker Pro reset;
- no broad refactor.

Use the smallest isolated manager/component family that satisfies the desired UX.



## 2026-09-09 follow-up — Entrance Text investigation and approved V1 plan

A separate read-only Codex investigation mapped the current entrance/welcome implementation in the real Unity scene before any changes were allowed.

Investigated current hierarchy:

```text
UIs/Other Toggles and Systems/Canvas (Welcome)
├── Collider_Entrance
├── Image
├── Image
├── Text (Kop)
├── Text (Alinea)
└── Button (Open)
```

Findings:
- `Text (Alinea)` is the current `TextMeshProUGUI` welcome body and is hardcoded in the Inspector;
- there is currently no entrance-specific UdonSharp/C# script, synced state or PlayerData;
- `Button (Open)` directly disables `Canvas (Welcome)`;
- because `Collider_Entrance` is under that Canvas, the same local action also opens the physical passage;
- visitor dismissal is local per player and should stay that way;
- the blocker works but its Canvas-relative transform is fragile, so it will not be moved during Entrance Text V1.

The agreed product model is deliberately split:

```text
PlayerData
= teacher's personal saved entrance text across visits/instances

synced instance state
= current entrance text for this running Classroom instance
```

The authorized host/teacher may edit the text multiple times during a session. Typing remains local; only explicit Apply / Save persists and publishes. The last published shared value must remain for players and late joiners even if the original host leaves, until the VRChat instance itself ends.

Authority must not be based on VRChat Master. Existing `VipAccessManager` is only an authorization dependency; `Networking.IsInstanceOwner` is the preferred automatic creator signal where supported. Group/Public/Build & Test may use a small explicit Claim/Start fallback. Rich takeover behaviour is not a V1 priority.

Planned minimal implementation:
- `EntranceTextManager.cs`;
- `EntranceTextEditorUI.cs`;
- one small manager object;
- one teacher-only editor panel;
- existing welcome Canvas, `Text (Alinea)`, Open button and collider preserved.

Initial validation target is roughly 400 characters / 9 explicit lines, plain text. Reset loads the default into the draft but still requires Apply / Save.

Stef confirmed a fresh full backup already exists and authorized Codex to proceed with a token-efficient V1 implementation. Real multiplayer, late-join and persistence acceptance must still be proven with real VRChat clients after implementation.

Canonical durable plan:

`ENTRANCE_TEXT_INVESTIGATION_PLAN_2026-09-09.md`
